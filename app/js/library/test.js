(function ($) {
	// The library folder is for JS libraries that we want to include, such as jquery UI. It is separate from files we create ourselves because we don't necessarily want gulp to concat and minimize these files.
	console.log("Test");

}(jQuery));
